# scripts/_archive/

Archived one-off utilities and investigation scripts.

Policy:
- Keep `scripts/` focused on supported operator workflows (`scripts/actions.py`) and required deterministic helpers.
- Move stale or brand-specific investigation scripts here instead of deleting them.

