# tests/

Unit tests for the deterministic decoder behavior (serial decoding + attribute decoding).

Run:
- `python3 -m unittest discover -s tests -p 'test_*.py' -q`

Optional (if you have pytest installed):
- `python3 -m pytest -q`
