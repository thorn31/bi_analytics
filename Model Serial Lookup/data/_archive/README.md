# data/_archive/

Archived one-off artifacts that are not part of the supported workflow.

Policy:
- Prefer keeping versioned artifacts in their canonical locations (e.g., `data/rules_normalized/`, `data/rules_discovered/`).
- Move scratch / investigation outputs here instead of deleting them.

